# Automatic-Message-with-python
very very very simple just a few lines of code

# Usage
copy the main.py codes or get it in linux or windows(any OS) IDE.
set your desired number of messages
set your message
Run the code and place the cursor on where you want to send messages.

**Note: You can modify sending speed or duration**
